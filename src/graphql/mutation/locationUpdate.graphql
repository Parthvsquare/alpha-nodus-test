mutation LocationUpdate(
  $locationUpdateId: String!
  $requestBody: LocationWriteInput!
  $tenant: String!
) {
  locationUpdate(
    id: $locationUpdateId
    requestBody: $requestBody
    tenant: $tenant
  ) {
    resourceID
  }
}
